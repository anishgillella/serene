[project]
  subdomain = "rag-q8jnl16x"  # Your LiveKit Cloud project subdomain

[agent]
  id = "CA_yuejkXotk5qC"  # Agent ID from LiveKit Cloud
  entrypoint = "app.agents.mediator_agent:mediator_entrypoint"  # Entrypoint function decorated with @server.rtc_session()

