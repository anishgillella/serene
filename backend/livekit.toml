[project]
  subdomain = "voice-agent-qnma0l98"  # Your LiveKit Cloud project subdomain

[agent]
  id = "A_aPV984RTQvBw"  # Agent ID from LiveKit Cloud
  entrypoint = "start_agent:router_entrypoint"  # Router entrypoint

